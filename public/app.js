const appRoot = document.querySelector('#app');
const TOKEN_KEY = 'eat-it-first-device-token';
const state = { view: 'home', family: null, foods: [], categories: [], locations: [], logs: [], authMode: 'create', joinLink: null, joinToken: null, error: '', filters: {}, inviteCode: null, photoDataUrl: null, photoFileName: null, expandedGroups: { categories: false, locations: false } };

const labels = { expired: '已过期', today: '今天到期', soon: '7 天内到期', normal: '保质期内', completed: '已完成' };
const actions = { created: '添加了', edited: '更新了', consumed: '消耗了 1 个', completed: '完成了', restored: '恢复了' };

function token() { return localStorage.getItem(TOKEN_KEY); }
async function api(path, options = {}) {
  const hasBody = Boolean(options.body);
  const response = await fetch(`./api${path}`, { headers: { ...(hasBody ? { 'Content-Type': 'application/json' } : {}), ...(token() ? { Authorization: `Bearer ${token()}` } : {}) }, ...options });
  const data = await response.json().catch(() => ({}));
  if (!response.ok) throw new Error(data.error || '请求失败');
  return data;
}
function escapeHtml(value) { return String(value ?? '').replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[char]); }
function showToast(message) { const node = document.createElement('div'); node.className = 'toast'; node.textContent = message; document.body.append(node); setTimeout(() => node.remove(), 2400); }
function syncDateFields() { document.querySelectorAll('.date-field input[type="date"]').forEach((input) => { const empty = !input.value; input.classList.toggle('empty', empty); input.classList.toggle('filled', !empty); }); }
function showConfirm(message) {
  return new Promise((resolve) => {
    const overlay = document.createElement('div');
    overlay.className = 'modal-overlay';
    overlay.innerHTML = `<div class="modal"><p class="modal-text">${escapeHtml(message)}</p><div class="modal-actions"><button class="button ghost" data-modal-cancel>取消</button><button class="button primary" data-modal-ok>确定</button></div></div>`;
    overlay.addEventListener('click', (event) => { if (event.target === overlay) close(false); });
    overlay.querySelector('[data-modal-cancel]').addEventListener('click', () => close(false));
    overlay.querySelector('[data-modal-ok]').addEventListener('click', () => close(true));
    document.body.append(overlay);
    function close(value) { overlay.remove(); resolve(value); }
  });
}
function dateText(date) { return new Intl.DateTimeFormat('zh-CN', { month: 'short', day: 'numeric', year: 'numeric' }).format(new Date(`${date}T12:00:00`)); }
function photoUrl(food) { return food.photo ? `./${food.photo.replace(/^\/+/, '')}` : null; }
function photoHtml(food) { const url = photoUrl(food); return url ? `<img class="food-photo" src="${escapeHtml(url)}" alt="${escapeHtml(food.name)}" loading="lazy">` : '<div class="food-photo empty" title="暂无照片"><span>📦</span></div>'; }

async function load() {
  const joinToken = new URLSearchParams(location.search).get('join');
  if (joinToken && !token()) { renderJoinLink(joinToken); return; }
  if (!token()) return renderAuth();
  try {
    const [family, foods, categories, locations, logs, join] = await Promise.all([api('/families/current'), api('/foods?includeCompleted=true'), api('/categories'), api('/locations'), api('/logs'), api('/families/join-link')]);
    state.family = family; state.foods = foods.foods; state.categories = categories.items; state.locations = locations.items; state.logs = logs.logs; state.joinToken = join.joinToken; render();
  } catch (error) { localStorage.removeItem(TOKEN_KEY); state.error = error.message; renderAuth(); }
}

function renderAuth() {
  appRoot.innerHTML = `<main class="auth-page"><section class="auth-card"><div class="eyebrow">家庭食品管理</div><h1>Eat It First</h1><p class="subtle">让家里的好食物流动起来，不浪费。</p>${state.error ? `<div class="notice">${escapeHtml(state.error)}</div>` : ''}<div class="auth-tabs"><button class="${state.authMode === 'create' ? 'active' : ''}" data-auth="create">创建家庭</button><button class="${state.authMode === 'join' ? 'active' : ''}" data-auth="join">加入家庭</button></div><form class="form" id="auth-form">${state.authMode === 'create' ? '<label>家庭名称<input name="name" required placeholder="陈的家"></label>' : '<label>邀请码<input name="inviteCode" required placeholder="10 位邀请码"></label>'}<label>你的名字<input name="nickname" required placeholder="小明"></label><button class="button primary" type="submit">${state.authMode === 'create' ? '创建家庭' : '加入家庭'}</button></form><p class="subtle auth-hint">${state.authMode === 'create' ? '如果家庭已存在，需通过家庭分享的加入链接或邀请码加入。' : '用同一个名字加入会自动归到同一身份，换设备不丢数据。'}</p></section></main>`;
  document.querySelectorAll('[data-auth]').forEach((button) => button.addEventListener('click', () => { state.authMode = button.dataset.auth; state.error = ''; renderAuth(); }));
  document.querySelector('#auth-form').addEventListener('submit', async (event) => { event.preventDefault(); const payload = Object.fromEntries(new FormData(event.currentTarget)); try { const data = await api(state.authMode === 'create' ? '/families' : '/families/join', { method: 'POST', body: JSON.stringify(payload) }); localStorage.setItem(TOKEN_KEY, data.deviceToken); if (data.inviteCode) state.inviteCode = data.inviteCode; await load(); if (data.inviteCode) showToast(`邀请码：${data.inviteCode}`); if (data.joined) showToast(`已以「${payload.nickname}」身份进入 ${data.family.name}`); } catch (error) { state.error = error.message; renderAuth(); } });
}

function renderJoinLink(joinToken) {
  state.joinLink = joinToken;
  appRoot.innerHTML = `<main class="auth-page"><div class="auth-brand"><strong>Eat It First</strong><span>你的家庭粮仓管家</span></div><section class="auth-card"><h1>加入家庭</h1>${state.error ? `<div class="notice">${escapeHtml(state.error)}</div>` : ''}<form class="form" id="join-link-form"><label>&nbsp;<input name="nickname" required placeholder="昵称"></label><button class="button primary" type="submit">加入家庭</button></form></section></main>`;
  document.querySelector('#join-link-form').addEventListener('submit', async (event) => {
    event.preventDefault();
    const nickname = new FormData(event.currentTarget).get('nickname');
    try {
      const data = await api('/families/join-link', { method: 'POST', body: JSON.stringify({ token: joinToken, nickname }) });
      localStorage.setItem(TOKEN_KEY, data.deviceToken);
      history.replaceState(null, '', location.pathname + location.search.replace(/[?&]join=[^&]*/, ''));
      state.joinLink = null;
      await load();
      showToast(`已以「${nickname}」身份进入 ${data.family.name}`);
    } catch (error) { state.error = error.message; renderJoinLink(joinToken); }
  });
}

function render() {
  const active = state.foods.filter((food) => food.status === 'active');
  const counts = { expired: active.filter((food) => food.expiryStatus === 'expired').length, today: active.filter((food) => food.expiryStatus === 'today').length, soon: active.filter((food) => food.expiryStatus === 'soon').length, normal: active.filter((food) => food.expiryStatus === 'normal').length };
  appRoot.innerHTML = `<div class="shell"><aside class="sidebar"><div class="brand"><strong>Eat It First</strong><small>${escapeHtml(state.family.family.name)}</small></div><nav class="nav">${navItems()}</nav><div class="sidebar-footer">${active.length} 个在架食品</div></aside><main class="main">${state.view === 'home' ? homeView(counts) : state.view === 'foods' ? foodsView() : state.view === 'add' ? addView() : familyView()}</main><nav class="mobile-nav">${navItems()}</nav></div>`;
  bindEvents();
}
function navItems() { return [['home', '首页'], ['foods', '全部食品'], ['add', '添加食品'], ['family', '家庭']].map(([view, label]) => `<button class="${state.view === view ? 'active' : ''}" data-view="${view}">${label}</button>`).join(''); }
function topbar(eyebrow, title, action = true) { return `<header class="topbar"><div><div class="eyebrow">${eyebrow}</div><h1>${title}</h1><p class="subtle">${new Intl.DateTimeFormat('zh-CN', { weekday: 'long', month: 'long', day: 'numeric' }).format(new Date())}</p></div>${action ? '<button class="button primary" data-view="add">添加食品</button>' : ''}</header>`; }
function homeView(counts) { const urgent = state.foods.filter((food) => food.status === 'active' && ['expired', 'today', 'soon'].includes(food.expiryStatus)).slice(0, 8); return `${topbar('今日关注', '粮食储备管理')}<section class="summary-grid"><button class="summary urgent" data-summary="expired"><span>已过期</span><b>${counts.expired}</b></button><button class="summary today" data-summary="today"><span>今天到期</span><b>${counts.today}</b></button><button class="summary soon" data-summary="soon"><span>7 天内到期</span><b>${counts.soon}</b></button><button class="summary" data-summary=""><span>在架批次</span><b>${state.foods.filter((food) => food.status === 'active').length}</b></button></section><section class="panel"><div class="panel-header"><h2>优先吃掉</h2><span class="subtle">${urgent.length} 项</span></div>${foodList(urgent)}</section>`; }
function foodsView() { const filters = state.filters; let foods = state.foods; if (filters.status === 'completed') foods = foods.filter((food) => food.status === 'completed'); else { foods = foods.filter((food) => food.status === 'active'); if (filters.status) foods = foods.filter((food) => food.expiryStatus === filters.status); } if (filters.categoryId) foods = foods.filter((food) => food.category_id === filters.categoryId); if (filters.locationId) foods = foods.filter((food) => food.location_id === filters.locationId); if (filters.q) foods = foods.filter((food) => food.name.toLowerCase().includes(filters.q.toLowerCase())); return `${topbar('库存', '全部食品')}<section class="panel"><div class="filters"><input data-filter="q" value="${escapeHtml(filters.q || '')}" placeholder="搜索食品"><select data-filter="status"><option value="">全部状态</option><option value="expired" ${filters.status === 'expired' ? 'selected' : ''}>已过期</option><option value="today" ${filters.status === 'today' ? 'selected' : ''}>今天到期</option><option value="soon" ${filters.status === 'soon' ? 'selected' : ''}>7 天内到期</option><option value="completed" ${filters.status === 'completed' ? 'selected' : ''}>已吃完</option></select><select data-filter="categoryId"><option value="">全部分类</option>${state.categories.filter((item) => !item.isDisabled).map((item) => `<option value="${item.id}" ${filters.categoryId === item.id ? 'selected' : ''}>${escapeHtml(item.name)}</option>`).join('')}</select><select data-filter="locationId"><option value="">全部位置</option>${state.locations.filter((item) => !item.isDisabled).map((item) => `<option value="${item.id}" ${filters.locationId === item.id ? 'selected' : ''}>${escapeHtml(item.name)}</option>`).join('')}</select></div>${foodList(foods)}</section>`; }
function foodList(foods) { if (!foods.length) return '<div class="empty">这里还没有食品，添加一批吧。</div>'; return `<div class="food-list">${foods.map((food) => `<article class="food-row">${photoHtml(food)}<div class="cell-name"><div class="food-name">${escapeHtml(food.name)}</div><div class="food-meta">${escapeHtml(food.category)} · ${escapeHtml(food.location)}</div></div><div class="cell-qty"><span class="pill">${food.quantity} 份</span></div><div class="cell-expiry"><span class="pill ${food.expiryStatus}">${labels[food.expiryStatus]}</span><div class="food-meta">${dateText(food.expiry_date)}</div></div><div class="cell-action">${food.status === 'active' ? `<button class="button secondary" data-consume="${food.id}">吃掉 1</button>` : '<span class="subtle">已完成</span>'}</div></article>`).join('')}</div>`; }
function addView() { return `${topbar('新批次', '添加食品', false)}<section class="panel"><form class="form" id="food-form"><div class="photo-upload"><label class="photo-picker"><input type="file" accept="image/*" capture="environment" data-photo-input hidden>${state.photoDataUrl ? `<img class="photo-preview" src="${state.photoDataUrl}" alt="照片预览">` : `<span class="photo-placeholder"><span class="photo-placeholder-icon">📷</span><span>拍照 / 选择照片</span></span>`}</label>${state.photoDataUrl ? '<button class="button ghost photo-remove" type="button" data-clear-photo>移除照片</button>' : ''}</div><div class="form-grid"><label>食品名称<input name="name" required placeholder="请输入食品名称"></label><label>数量<input name="quantity" type="number" min="0" value="1" required></label><label>分类<select name="categoryId" required>${state.categories.filter((item) => !item.isDisabled).map((item) => `<option value="${item.id}">${escapeHtml(item.name)}</option>`).join('')}</select></label><label>存放位置<select name="locationId" required>${state.locations.filter((item) => !item.isDisabled).map((item) => `<option value="${item.id}">${escapeHtml(item.name)}</option>`).join('')}</select></label><label>生产日期（选填）<span class="date-field"><input name="producedDate" type="date"><span class="date-hint">YYYY/MM/DD</span></span></label><label>保质期（选填）<span class="shelf-life"><input name="shelfLife" type="number" min="0" step="1" placeholder="时长"><select name="shelfLifeUnit"><option value="day">天</option><option value="month" selected>月</option><option value="year">年</option></select></span></label><label>到期日期<span class="date-field"><input name="expiryDate" type="date" required><span class="date-hint">YYYY/MM/DD</span></span></label></div><label>备注<textarea name="note" placeholder="可选备注"></textarea></label><input type="hidden" name="photo" value="${escapeHtml(state.photoFileName || '')}"><div class="form-actions"><button class="button ghost" type="button" data-view="home">取消</button><button class="button primary">保存食品</button></div></form></section>`; }
function familyView() { const joinUrl = state.joinToken ? `${location.origin}${location.pathname}?join=${state.joinToken}` : ''; return `${topbar('家庭', escapeHtml(state.family.family.name), false)}<div class="settings-grid"><section class="panel"><div class="panel-header"><h2>成员</h2></div>${state.family.members.map((member) => `<div class="member"><b>${escapeHtml(member.nickname)}${member.id === state.family.currentMemberId ? ' · 你' : ''}</b><span class="subtle">${new Date(member.joinedAt).toLocaleDateString('zh-CN')}</span></div>`).join('')}<div class="panel-header" style="margin-top:26px"><h2>加入链接</h2><button class="button secondary" data-rotate-link>新链接</button></div><p class="subtle" style="margin:0 0 12px">分享这个链接给家人，对方打开后只需输入名字就能加入，一个链接可在多台设备重复使用。</p><div class="join-box">${joinUrl ? `<span class="join-url">${escapeHtml(joinUrl)}</span><button class="button ghost" data-copy="${escapeHtml(joinUrl)}">复制</button>` : '<span class="subtle">链接加载中…</span>'}</div><div class="token-box" style="margin-top:18px">邀请码：${state.inviteCode || '已隐藏'}${state.inviteCode ? `<button class="button ghost" data-copy="${state.inviteCode}">复制</button><button class="button ghost" data-rotate>新邀请码</button>` : ''}</div></section><section class="panel"><div class="panel-header collapse-header" data-toggle-group="categories"><h2>分类</h2><span class="collapse-tools"><span class="subtle collapse-count">${state.categories.length} 项</span><button class="button secondary" data-add-option="categories" data-stop-propagation>添加</button><button class="collapse-chevron">${state.expandedGroups.categories ? '收起 ▲' : '展开 ▼'}</button></span></div><div class="collapse-body${state.expandedGroups.categories ? '' : ' collapsed'}">${optionList(state.categories, 'categories')}</div><div class="panel-header collapse-header" data-toggle-group="locations" style="margin-top:25px"><h2>存放位置</h2><span class="collapse-tools"><span class="subtle collapse-count">${state.locations.length} 项</span><button class="button secondary" data-add-option="locations" data-stop-propagation>添加</button><button class="collapse-chevron">${state.expandedGroups.locations ? '收起 ▲' : '展开 ▼'}</button></span></div><div class="collapse-body${state.expandedGroups.locations ? '' : ' collapsed'}">${optionList(state.locations, 'locations')}</div></section><section class="panel"><div class="panel-header"><h2>最近动态</h2></div><div class="activity">${state.logs.slice(0, 8).map((log) => `<div class="activity-item"><div class="activity-row"><b>${escapeHtml(log.nickname)}</b> ${actions[log.action] || log.action} <b>${escapeHtml(log.food_name)}</b>${log.action === 'consumed' ? `<button class="button ghost undo" data-undo="${log.id}">撤销</button>` : ''}</div><small>${new Date(log.created_at).toLocaleString('zh-CN')}</small></div>`).join('') || '<div class="empty">暂无动态。</div>'}</div></section></div>`; }
function optionList(items, table) { return items.length ? `<div class="option-list">${items.map((item) => `<div class="option-chip${item.isDisabled ? ' disabled' : ''}"><span class="option-chip-name">${escapeHtml(item.name)}${item.isDefault ? '<em class="option-tag">默认</em>' : ''}${item.isDisabled ? '<em class="option-tag">已停用</em>' : ''}</span><span class="option-chip-actions"><button class="button ghost" data-disable="${table}|${item.id}|${item.isDisabled ? 'false' : 'true'}">${item.isDisabled ? '启用' : '停用'}</button>${item.isDefault ? '' : `<button class="button danger" data-delete-option="${table}|${item.id}">删除</button>`}</span></div>`).join('')}</div>` : '<p class="subtle">暂无选项，点击右上角添加。</p>'; }
function bindEvents() { document.querySelectorAll('[data-view]').forEach((button) => button.addEventListener('click', () => { state.view = button.dataset.view; render(); })); document.querySelectorAll('[data-summary]').forEach((button) => button.addEventListener('click', () => { state.filters.status = button.dataset.summary || ''; state.view = 'foods'; render(); })); document.querySelectorAll('[data-consume]').forEach((button) => button.addEventListener('click', async () => { button.disabled = true; try { await api(`/foods/${button.dataset.consume}/consume`, { method: 'POST' }); await load(); showToast('已更新'); } catch (error) { showToast(error.message); button.disabled = false; } })); const foodForm = document.querySelector('#food-form'); if (foodForm) foodForm.addEventListener('submit', async (event) => { event.preventDefault(); try { const payload = Object.fromEntries(new FormData(foodForm)); if (payload.expiryDate) payload.expiryDate = payload.expiryDate.replace(/\//g, '-'); if (state.photoDataUrl && !state.photoFileName) { const up = await api('/photos', { method: 'POST', body: JSON.stringify({ data: state.photoDataUrl }) }); payload.photo = up.url; } await api('/foods', { method: 'POST', body: JSON.stringify(payload) }); state.photoDataUrl = null; state.photoFileName = null; state.view = 'home'; await load(); showToast('食品已添加'); } catch (error) { showToast(error.message); } }); const photoInput = document.querySelector('[data-photo-input]'); if (photoInput) photoInput.addEventListener('change', async (event) => { const file = event.target.files[0]; if (!file) return; const compressed = await compressImage(file); state.photoDataUrl = compressed; render(); }); document.querySelectorAll('.date-field input[type="date"]').forEach((input) => input.addEventListener('click', () => { try { input.showPicker(); } catch (error) { } })); document.querySelectorAll('.date-field input[type="date"]').forEach((input) => input.addEventListener('change', syncDateFields)); const producedInput = document.querySelector('input[name="producedDate"]'); const shelfLifeInput = document.querySelector('input[name="shelfLife"]'); const shelfLifeUnit = document.querySelector('select[name="shelfLifeUnit"]'); const calcExpiry = () => { const producedValue = producedInput?.value; const shelfValue = shelfLifeInput?.value; const expiryDate = document.querySelector('input[name="expiryDate"]'); if (!expiryDate) return; expiryDate.readOnly = Boolean(producedValue); if (!producedValue) { syncDateFields(); return; } if (!shelfValue) { expiryDate.value = ''; syncDateFields(); return; } const produced = new Date(`${producedValue}T12:00:00`); const amount = Number(shelfValue); if (!Number.isFinite(amount) || amount < 0) return; const unit = shelfLifeUnit?.value || 'day'; if (unit === 'day') produced.setDate(produced.getDate() + amount); else if (unit === 'month') produced.setMonth(produced.getMonth() + amount); else if (unit === 'year') produced.setFullYear(produced.getFullYear() + amount); const pad = (n) => String(n).padStart(2, '0'); expiryDate.value = `${produced.getFullYear()}-${pad(produced.getMonth() + 1)}-${pad(produced.getDate())}`; syncDateFields(); }; if (producedInput && shelfLifeInput) [producedInput, shelfLifeInput, shelfLifeUnit].forEach((input) => input.addEventListener('change', calcExpiry)); syncDateFields(); document.querySelectorAll('[data-clear-photo]').forEach((button) => button.addEventListener('click', () => { state.photoDataUrl = null; state.photoFileName = null; render(); })); document.querySelectorAll('[data-filter]').forEach((input) => input.addEventListener('change', () => { state.filters[input.dataset.filter] = input.type === 'checkbox' ? input.checked : input.value; render(); })); document.querySelectorAll('[data-rotate]').forEach((button) => button.addEventListener('click', async () => { const data = await api('/families/invite-code/rotate', { method: 'POST' }); state.inviteCode = data.inviteCode; render(); })); document.querySelectorAll('[data-rotate-link]').forEach((button) => button.addEventListener('click', async () => { const data = await api('/families/join-link/rotate', { method: 'POST' }); state.joinToken = data.joinToken; render(); showToast('已生成新链接'); })); document.querySelectorAll('[data-undo]').forEach((button) => button.addEventListener('click', async () => { if (!(await showConfirm('确定撤销这次消耗？'))) return; button.disabled = true; try { await api(`/logs/${button.dataset.undo}/undo`, { method: 'POST' }); await load(); showToast('已撤销'); } catch (error) { showToast(error.message); button.disabled = false; } })); document.querySelectorAll('[data-copy]').forEach((button) => button.addEventListener('click', () => navigator.clipboard?.writeText(button.dataset.copy).then(() => showToast('邀请码已复制')))); document.querySelectorAll('[data-add-option]').forEach((button) => button.addEventListener('click', async (event) => { event.stopPropagation(); const name = prompt(`新${button.dataset.addOption === 'categories' ? '分类' : '存放位置'}名称`); if (!name) return; try { await api(`/${button.dataset.addOption}`, { method: 'POST', body: JSON.stringify({ name }) }); await load(); state.expandedGroups[button.dataset.addOption] = true; state.view = 'family'; render(); } catch (error) { showToast(error.message); } })); document.querySelectorAll('[data-toggle-group]').forEach((header) => header.addEventListener('click', (event) => { const group = header.dataset.toggleGroup; if (event.target.closest('[data-stop-propagation]')) return; state.expandedGroups[group] = !state.expandedGroups[group]; render(); })); document.querySelectorAll('[data-disable]').forEach((button) => button.addEventListener('click', async () => { const [table, id, disabled] = button.dataset.disable.split('|'); await api(`/${table}/${id}`, { method: 'PATCH', body: JSON.stringify({ disabled: disabled === 'true' }) }); await load(); state.view = 'family'; render(); })); document.querySelectorAll('[data-delete-option]').forEach((button) => button.addEventListener('click', async () => { const [table, id] = button.dataset.deleteOption.split('|'); if (!(await showConfirm('确定删除该选项？'))) return; try { await api(`/${table}/${id}`, { method: 'DELETE' }); await load(); state.view = 'family'; render(); showToast('已删除'); } catch (error) { showToast(error.message); } })); }

function compressImage(file, maxWidth = 1280, quality = 0.8) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onerror = () => reject(new Error('无法读取图片'));
    reader.onload = () => {
      const img = new Image();
      img.onerror = () => reject(new Error('无法解析图片'));
      img.onload = () => {
        const scale = Math.min(1, maxWidth / img.width);
        const canvas = document.createElement('canvas');
        canvas.width = Math.round(img.width * scale);
        canvas.height = Math.round(img.height * scale);
        const ctx = canvas.getContext('2d');
        ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
        resolve(canvas.toDataURL('image/jpeg', quality));
      };
      img.src = reader.result;
    };
    reader.readAsDataURL(file);
  });
}

load();